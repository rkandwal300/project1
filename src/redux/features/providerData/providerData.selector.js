export const selectProviderList = (state) => state.provider.providerList;
export const selectCurrentProviderType = (state) => state.provider.type;
export const selectCurrentProviderRegions = (state) => state.provider.regions;
export const selectCurrentProviderInstanceTypes = (state) => state.provider.instanceTypes;
export const selectCurrentProviderPricingModels = (state) => state.provider.pricingModels;
export const selectCurrentProviderName = (state) => state.provider.name;
export const selectCurrentProviderTelemetryCloud = (state) => state.provider.telemetryCloud;